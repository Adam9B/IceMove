{% extends 'base.html.twig' %}

{% block title %}Éditer le Programme{% endblock %}

{% block body %}
    <h1>Modifier le Programme</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('app_programme_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}