{# templates/sceance/new.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer une nouvelle séance</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_accueil') }}"> retour accueil</a>
    <a href="{{ path('app_sceance_index') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}